spring:
  application:
    name: sportsync-backend
  datasource:
    url: ${DB_URL:jdbc:postgresql://db.cllduivwjdphgdgcfsbd.supabase.co:5432/postgres}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:SportSync123!}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update # Automatically generate tables for development
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080
  error:
    include-message: always

jwt:
  secret: pkRDIGmmaPsc3zPMpiJvdNIQtMMUVSpSCOeTMRDREU/JI/kJ1XpX4Jz5I5udZLmQEBYthJaL4yRvuFIKrkM08w==
  expiration: 86400000 # 24 hours in ms
